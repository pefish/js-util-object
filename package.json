{
  "name": "@pefish/js-util-object",
  "version": "0.0.1",
  "main": "./lib/object.js",
  "types": "./types/*.d.ts",
  "scripts": {
    "test": "mocha --require ts-node/register src/object.test.ts",
    "build": "rm -rf lib types && tsc",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "prepublish": "yarn build"
  },
  "dependencies": {

  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.7",
    "mocha": "^6.1.4",
    "ts-node": "^8.1.0",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
